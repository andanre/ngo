// import axios from 'axios';
// import { Article } from '../types';

// const API_URL = 'http://localhost:5000/news-berita';

// export const fetchArticles = () => {
//   return axios.get<Article[]>(API_URL);
// };

// export const fetchArticleById = (id: number) => {
//   return axios.get<Article>(`${API_URL}/${id}`);
// };

// export const createArticle = (article: Partial<Article>) => {
//   return axios.post<Article>(API_URL, article);
// };

// export const updateArticle = (id: number, article: Partial<Article>) => {
//   return axios.put<Article>(`${API_URL}/${id}`, article);
// };

// export const deleteArticle = (id: number) => {
//   return axios.delete<Article>(`${API_URL}/${id}`);
// }

